import React from 'react';
import { useQuery } from '@apollo/client';




const Homebar = () => {
 
  return (
    <main>
      <div className="flex-row justify-center">
     This is where we'll render the home bar page
      </div>
      
    </main>
  );
};

export default Homebar;
